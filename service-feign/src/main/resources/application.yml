server:
  port: 18786

spring:
  application:
    name: service-feign

eureka:
  client:
    service-url:
      defaultZone: http://root:root@localhost:18761/eureka/

# feign自带断路器，但是默认是关闭的
feign:
  hystrix:
    enabled: true
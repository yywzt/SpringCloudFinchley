spring:
  application:
    name: config-client,zuul
  cloud:
    config:
      # 指明远程仓库的分支
      label: master
      profile: dev
      # 指明配置服务中心的网址
      uri: http://localhost:8088/

server:
  port: 8089
